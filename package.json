{
  "name": "newcomputer",
  "private": true,
  "type": "module",
  "scripts": {
    "nc": "tsx apps/worker/src/cli.ts",
    "ui": "pnpm --filter @nc/ui build && tsx apps/ui/src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json && tsc -p apps/ui/tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
