# Postgres connection — local dev container or Neon.
# Local docker: postgres://personalbase_admin:personalbase-admin-local@127.0.0.1:5432/personalbase
# Neon: postgres://<user>:<password>@<endpoint>.neon.tech/<db>?sslmode=require
DATABASE_URL=

# Needed by the paper-filter reactor (LLM calls).
ANTHROPIC_API_KEY=
